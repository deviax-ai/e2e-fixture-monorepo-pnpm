# Hoist all deps to the workspace root node_modules. pnpm's default
# isolation uses symlinks that don't survive a Kaniko COPY across
# stages — the runner stage ends up with apps/web/node_modules linking
# at non-existent paths. Hoisting trades disk space for compatibility.
shamefully-hoist=true
node-linker=hoisted
